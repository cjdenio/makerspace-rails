💰 Volunteer discount applied for <<%= profile_url %>|<%= full_name %>> (ID: <%= member_id %>) — *<%= amount %>* for <%= billing_cycles %> (<%= total_cycles %> total queued). Discount: <%= discount_description %>
