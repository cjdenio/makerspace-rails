⚠️ Volunteer discount earned by <<%= profile_url %>|<%= full_name %>> (ID: <%= member_id %>) but no active subscription found. Credits preserved — will apply automatically once a subscription is activated.
