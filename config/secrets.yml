
development:
  secret_key_base: <%= ENV.fetch("SECRET_KEY_BASE", "development_secret_key_base") %>

test:
  secret_key_base: <%= ENV.fetch("SECRET_KEY_BASE", "test_secret_key_base") %>

  # Do not keep production secrets in the repository,
  # instead read values from the environment.
production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
